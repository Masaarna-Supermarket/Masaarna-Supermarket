/* ═══════════════════════════════════════════════════════════════════════
   Masaarna — the ONE setting you ever change by hand.

   Paste the address Cloudflare gave you between the quotes below, save,
   and upload this one file. After that you never edit or upload a file
   again — the admin page publishes straight to the live shop.

   It looks like:  https://masaarna.YOUR-NAME.workers.dev
   No slash at the end.
   ═══════════════════════════════════════════════════════════════════════ */

window.MASAARNA_API = "";

/* Leave it empty and the shop falls back to reading products.json from this
   folder, exactly as it did before. Nothing breaks — it is just manual again. */
